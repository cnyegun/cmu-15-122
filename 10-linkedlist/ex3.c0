elem ith(list* l, int i)
//@requires i >= 0;
{
    list* idx = l;
    int count = 0;

    while (idx != NULL)
    {
        if (count == i) {
            return idx->data;
        }
        
        count++;
        idx = idx->next;
    }

    //@assert false;
}